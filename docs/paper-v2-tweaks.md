# Paper v2 tweak tracker

arXiv v1 submitted 2026-07-05 (on hold; letting it play out). This file
collects everything queued for the v2 replacement. Add items freely; mark ☑
when applied to `paper-draft.md`.

## Done (already in draft, post-v1)
- ☑ Internal "Draft for review…" note removed from page 1 (naming convention
  moved to §13 opening; provenance note now a non-rendering comment).
- ☑ "uinvolved" typo (§12 footnote).
- ☑ √ glyph: `√7 ≈ 19.1°` conflated scale with rotation AND the `\surd`
  mapping rendered poorly — now "scales by $\sqrt{7}$ and rotates by ≈ 19.1°".
  (If other stray unicode-math glyphs turn up, prefer real math mode over the
  `newunicodechar` fallback.)
- ☑ Introductory hero image: Blue Marble butterfly (ex0097, L2) added after
  the first Introduction paragraph — first 9 pages previously had no imagery.
- ☑ Date June → July 2026.

## Done (2026-07-06 batch)
- ☑ §8 Fig 2 (f2.pdf): width 55% → 100% (SVG is 658×110, very wide aspect —
  55% made it tiny; pandoc figures are centred, the off-centre look was the
  grey left panel + small size).
- ☑ Margin overflow from long `\texttt` paths: `\usepackage[htt]{hyphenat}`.
- ☑ Footnotes now ragged-right (package-free `\@makefntext` patch in
  header.tex) — fixes stretch-justified footers (p11).
- ☑ Removed the two self-citing "Source figure paper_figures/fNN.png"
  footnotes (f20, f21). Kept all "Generated by <script>" footnotes —
  they carry the reproducibility claim.
- ☑ Appendix A "Almost-everywhere bijectivity" rewritten: previous text
  conflated point-level boundary ambiguity (the 0.4999… analogue, measure
  zero, unavoidable) with cell-level split ambiguity (positive measure if
  unresolved; killed by the mode-0 convention). Now stated as two separate
  failure sources + surjectivity noted.

## Pending
- ☑ §10b/§10c id/label nomenclature (settled 2026-07-06). Agreed trichotomy:
  locator (10a) / key = bin = identity, ID = key at L30 modulo tail marker
  (10b) / label = human-readable serialisation of the key, `<body>.<tail>` per
  h9_label, bijective with keys (10c rewritten). Bare digit body demoted to
  "lineage path" — fails as a name in BOTH directions (split cells: one cell
  two bodies; p_c2/r_mo collisions: one body two cells). Old §10c's "mode-0
  enclosure, no tail at all" claim contradicted §10b's own collision examples
  and diverged from h9_label (which keeps the tail); removed. London table +
  figure caption now say "digit body (lineage)".
- ☑ Glossary synced (2026-07-06): labels/lineage-path subsection added; OGC/ISO
  tables replaced with the spec-verified versions (clause numbers, C/A/—);
  "(per Even Rouault)" attribution REMOVED (private conversation, public file);
  stale "sub-face identifier" legacy row marked dropped.
- ☑ Fig 14 Tissot regenerated: hex strokes thinner (0.55/0.35/0.18) at 0.45
  opacity, `vector_effect='non-scaling-stroke'` removed (the 100%-zoom
  thickening culprit), stroke colour green → slate blue (#33506b — the old
  dark green sat against red Tissot fills: r/g pairing), graticule 0.3 @ 0.55.
- ☑ Footnote sweep: all remaining footnotes are script-provenance (kept by
  design); one half-self-citation (f5 path) trimmed.
- ☑ F12 slot decided: keep the tuned ±0.25% continuous NP view (continuous
  field / Mollweide pattern / census classification = three distinct
  registers); global snow-globe idea superseded.

## Author read-through (in progress)
Ben: full offline pass of v2 with highlighter, 2026-07-06 → . Findings land
here as a new section when done; docs/paper.pdf (line-numbered build) is the
annotation copy. If edits result → v3 replacement via the usual package steps.

## v2 COMPLETE (2026-07-06)
Built: paper.pdf (42 pp, clean), arxiv/main.tex (standalone-compile verified),
docs/hex9-arxiv-v2.zip (22 files, 9.6 MB). Content probes all pass.
Remaining before upload: COMMIT the working tree + retag (paper_v2), then
follow arxiv/SUBMISSION_NOTES.md §v2 (on-hold → ask moderation to return;
announced → replace).
- ☑ F12 (ex0081wau_5) reworked collaboratively (2026-07-06): strokes visible
  (lw 0.125, opaque grey), FILL_ALPHA 0.60, cap tightened ±1% → ±0.25%
  (lim_pct=1/4), vertex at lower-left with both seam rays grading across the
  frame; geodesic-area cache added (output/ex0081_cache_5.npz) so re-renders
  take ~1 min. Caption updated (±0.25%, NP-vertex view, width 58%→75%).
- ☑ Fig 17: panel (a) removed (now the intro hero), caption cross-refs the
  Introduction; hero's 1-px top/left border trimmed.
- ☑ NP crop strip (F12c) regenerated with visible tessellation, centred on
  the vertex.
- ☑ F12c REPLACED (2026-07-06) by the vertex-census three-panel figure —
  NEW FINDING folded into §11b: the six vertices form THREE pairwise-exact
  classes (NP/SP 48 cells >0.1%, 12 >1%, −3.57/+4.80%; 0°/180° 38, 2, −2.10%;
  90°E/W 36, 2, −2.60%; spectra identical within pairs to >3 decimals →
  structural). Sign flip (polar extremes excess, equatorial wholly deficit);
  rays are meridional-only everywhere. New example examples/ex0124_vertex_census.py
  (census table + figure; shares ex0081 cache); figure at
  paper_figures/ex0124_vertex_census_web.jpg. §11b prose extended; mechanism
  left open pending raw-AK check. Mollweide caption now states exact clip
  (−0.31%/+0.26%). Figures index updated.
- ☑ Raw-AK control run DONE (2026-07-06, `ex0124 --raw`, `b_oct.no_warp()`):
  raw AK already splits all three classes pairwise-exactly (NP 20.77, 0/180
  20.253/.248/.247…, 90E/W 20.253/.251/.250… — ~0.005% apart in a ±20% field);
  warp magnifies equatorial-pair separation ~100× (to ~0.5%). Combinatorial
  seed verified at L0: two root cells per vertex, join axis diagonal (poles) /
  meridional (0/180) / equatorial (90E/W) — Ben's read; f21 shows it. §11b
  mechanism sentence promoted from "candidate" to stated, with footnoted
  control (raw: only 4,440 of 708,588 within 0.1% vs 244 warped).
- ☐ Paper two: seam trade-off analysis (measured 2026-07-06 from cache):
  equator seams close area (med 0.0006%) with the largest shear tail
  (p95 aspect 1.534); meridian seams carry 2–6× area residual with the
  tightest shape spread (p95 1.486); interior med aspect 1.397 (cf. 1.371
  survey mean). Conformance-vs-authalicity contrast is a tail phenomenon.

## When v2 goes up
1. Rebuild: `make -f Makefile.paper` (PDF) and regenerate `arxiv/main.tex` +
   re-strip `\linenumbers` + rezip (see `arxiv/SUBMISSION_NOTES.md`).
2. Inspect the new page 1-2 (hero figure placement) and §12 (sqrt fix).
3. Commit + consider re-tagging (`paper_v2`).
4. Replace on arXiv (same ID, becomes v2); update JOSIS materials if already
   sent.
